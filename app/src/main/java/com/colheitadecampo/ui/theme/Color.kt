package com.colheitadecampo.ui.theme

import androidx.compose.ui.graphics.Color

// Cores principais da marca - Ajustadas para melhor visibilidade
val BluePrimary = Color(0xFF083E59) // Versão mais saturada do Pantone 540 C para maior visibilidade
val BlueMedium = Color(0xFF0052DB) // Versão mais saturada do Pantone 7691 C
val TerracotaAccent = Color(0xFFCE8D64) // Versão mais clara do Pantone 722 C para melhor contraste
val LimeAccent = Color(0xFFCBD500) // Versão mais brilhante do Pantone 390 C

// Cores neutras - Com contraste aumentado
val NeutralBlack = Color(0xFF000000) // Pantone Black C
val NeutralGray = Color(0xFF6D6E71) // Versão mais escura do Pantone Cool Gray 8 C para melhor visibilidade em fundo claro
val NeutralLightGray = Color(0xFFE0E0E0) // Versão ligeiramente mais clara do Pantone 25% Black C

// Cores funcionais otimizadas para alta visibilidade em luz solar
val Success = Color(0xFF00C853) // Verde mais saturado para itens colhidos
val Error = Color(0xFFFF1744) // Vermelho mais brilhante para erros
val White = Color(0xFFFFFFFF) // Branco para texto sobre cores escuras
val OffWhite = Color(0xFFF5F5F5) // Quase branco para fundos
val BlackText = Color(0xFF121212) // Preto suavizado para textos

// Cores de alta visibilidade para condições externas
val HighVisibilityYellow = Color(0xFFFFEB3B) // Amarelo de alta visibilidade
val HighVisibilityOrange = Color(0xFFFF9800) // Laranja de alta visibilidade
val HighContrastBlue = Color(0xFF2962FF) // Azul de alto contraste

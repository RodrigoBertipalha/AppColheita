package com.colheitadecampo.ui.theme

import androidx.compose.ui.graphics.Color

// Cores principais da marca
val BluePrimary = Color(0xFF092A3B) // Pantone 540 C
val BlueMedium = Color(0xFF003DA6) // Pantone 7691 C
val TerracotaAccent = Color(0xFFB67F5A) // Pantone 722 C
val LimeAccent = Color(0xFFB4BD00) // Pantone 390 C

// Cores neutras
val NeutralBlack = Color(0xFF000000) // Pantone Black C
val NeutralGray = Color(0xFF898A8D) // Pantone Cool Gray 8 C
val NeutralLightGray = Color(0xFFD0D0D0) // Pantone 25% Black C

// Cores funcionais (necessárias para o funcionamento do app)
val Success = Color(0xFF4CAF50) // Verde para itens colhidos
val Error = Color(0xFFF44336) // Vermelho para erros
val White = Color(0xFFFFFFFF) // Branco para texto sobre cores escuras
